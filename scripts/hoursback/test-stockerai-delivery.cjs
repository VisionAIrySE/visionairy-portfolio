#!/usr/bin/env node
'use strict';
const {PrismaClient}=require('@prisma/client');const R=require('../../src/hoursback/crm/productReadiness.js');const D=require('../../src/hoursback/crm/productDelivery.js');const {resendProvider}=require('../../src/hoursback/crm/productResend.js');
const TEST_TO='russ@visionairy.biz',PLACE_ID='stockerai-delivery-test-russ',SIGNATURE='Best regards,\n\nRuss Wright\nFounder, StockerAI\n503-621-8000\nruss@visionairy.biz\nhttps://www.stocker-ai.com/';
async function prepare(db){return db.$transaction(async tx=>{const product=await tx.cRMProduct.update({where:{id:'stockerai'},data:{sendingEnabled:true,senderEmail:'russ@visionairy.biz',replyToEmail:'russ@reply.visionairy.biz',senderVerifiedAt:new Date(),replyRouteVerifiedAt:new Date(),signatureText:SIGNATURE,timeZone:'America/Los_Angeles',localSendTime:'10:00'}});const prospect=await tx.prospect.upsert({where:{placeId:PLACE_ID},create:{placeId:PLACE_ID,name:'StockerAI delivery and reply test',email:TEST_TO,siteStatus:'READ',siteReadAt:new Date(),theirWork:'verify StockerAI email delivery and reply tracking'},update:{email:TEST_TO,doNotContact:false,emailBouncedAt:null}});const membership=await tx.productProspect.upsert({where:{productId_prospectId:{productId:'stockerai',prospectId:prospect.id}},create:{productId:'stockerai',prospectId:prospect.id},update:{archivedAt:null}});let reading=await tx.reading.findFirst({where:{prospectId:prospect.id,source:'website',reader:'understand-businesses',outcome:'read'}});if(!reading)reading=await tx.reading.create({data:{prospectId:prospect.id,source:'website',reader:'understand-businesses',readerVersion:'delivery-test',outcome:'read',finishedAt:new Date(),pages:{create:{url:'https://www.stocker-ai.com/',text:'StockerAI delivery and reply routing test record.',bytes:48}}}});let finding=await tx.finding.findFirst({where:{prospectId:prospect.id,readingId:reading.id,field:'theirWork',retiredAt:null}});if(!finding)finding=await tx.finding.create({data:{prospectId:prospect.id,readingId:reading.id,field:'theirWork',value:'verify StockerAI email delivery and reply tracking',status:'confirmed',url:'https://www.stocker-ai.com/',quote:'StockerAI delivery and reply routing test record.'}});const recipient=await tx.productRecipient.upsert({where:{membershipId_recipientKey:{membershipId:membership.id,recipientKey:'inbox'}},create:{productId:'stockerai',membershipId:membership.id,recipientKey:'inbox',email:TEST_TO,selected:true},update:{email:TEST_TO,selected:true,repliedAt:null}});const sequence=await tx.productSequence.findFirst({where:{productId:'stockerai',approvedAt:{not:null}},orderBy:{version:'desc'}});if(!sequence)throw Error('The approved StockerAI sequence is missing');const enrollment=await tx.productEnrollment.upsert({where:{recipientId_sequenceId:{recipientId:recipient.id,sequenceId:sequence.id}},create:{productId:'stockerai',recipientId:recipient.id,sequenceId:sequence.id},update:{}});if(enrollment.startedAt||enrollment.stoppedAt)throw Error('The delivery test was already used; audit it before another test');const bodies=[
 'Hi Russ,\n\nThis is the controlled StockerAI delivery and reply-routing test. It uses the same sender, reply address, rendering, signature, provider adapter, and saved delivery tracking planned for operator outreach. Please inspect the formatting and links, then reply with the words "StockerAI reply test." That reply should reach your normal inbox and automatically stop only this test campaign.\n\nLive demo: https://www.stocker-ai.com/demo',
 'Hi Russ,\n\nThis unsent test placeholder exists only so the temporary campaign has the approved five-message structure. It must never be delivered.\n\nLive demo: https://www.stocker-ai.com/demo',
 'Hi Russ,\n\nThis unsent test placeholder exists only so the temporary campaign has the approved five-message structure. It must never be delivered.\n\nLive demo: https://www.stocker-ai.com/demo',
 'Hi Russ,\n\nThis unsent test placeholder exists only so the temporary campaign has the approved five-message structure. It must never be delivered.\n\nLive demo: https://www.stocker-ai.com/demo',
 'Hi Russ,\n\nThis unsent test placeholder exists only so the temporary campaign has the approved five-message structure. It must never be delivered.\n\nLive demo: https://www.stocker-ai.com/demo'];
 for(let index=0;index<5;index++)await tx.productMessage.upsert({where:{enrollmentId_touch:{enrollmentId:enrollment.id,touch:index+1}},create:{productId:'stockerai',enrollmentId:enrollment.id,touch:index+1,subject:index===0?'StockerAI delivery and reply test':'StockerAI unsent test placeholder '+(index+1),body:bodies[index],evidenceFindingIds:[finding.id]},update:{subject:index===0?'StockerAI delivery and reply test':'StockerAI unsent test placeholder '+(index+1),body:bodies[index],evidenceFindingIds:[finding.id]}});return {product,enrollmentId:enrollment.id};});}
async function main(){if(!process.argv.includes('--send')||!process.argv.includes('--confirm-to='+TEST_TO)){console.log(JSON.stringify({mode:'PREVIEW',to:TEST_TO,productionChanges:['configure the approved StockerAI sender and signature','create one clearly named test campaign','select and release only the test recipient'],realEmails:1,customerEmails:0,requiredArguments:['--send','--confirm-to='+TEST_TO]},null,2));return;}if(!process.env.RESEND_API_KEY)throw Error('Resend sending access is not configured');const db=new PrismaClient();try{const prepared=await prepare(db),now=new Date(),date=R.localClock(now,prepared.product.timeZone).date;const campaign=await R.loadCampaign(db,'stockerai',prepared.enrollmentId);if(!campaign.releasedAt)await D.releaseCampaign(db,{productId:'stockerai',enrollmentId:prepared.enrollmentId,firstLocalDate:date,now});const provider=resendProvider({apiKey:process.env.RESEND_API_KEY,inboundKey:process.env.RESEND_INBOUND_API_KEY});const result=await D.attemptCampaign(db,{productId:'stockerai',enrollmentId:prepared.enrollmentId,send:provider.send,now});console.log(JSON.stringify({to:TEST_TO,enrollmentId:prepared.enrollmentId,result},null,2));}finally{await db.$disconnect();}}
if(require.main===module)main().catch(error=>{console.error(error.message);process.exitCode=1;});
module.exports={prepare,SIGNATURE,TEST_TO};
