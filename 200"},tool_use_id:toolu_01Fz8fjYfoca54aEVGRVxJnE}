◈ XFFI  A plan is marked active but cannot be read — nothing is being kept in scope. Re-run /distill, or clear the stale marker.
◈ XFFI  A plan is marked active but cannot be read — nothing is being kept in scope. Re-run /distill, or clear the stale marker.
◈ XFFI  A plan is marked active but cannot be read — nothing is being kept in scope. Re-run /distill, or clear the stale marker.
◈ XFFI  A plan is marked active but cannot be read — nothing is being kept in scope. Re-run /distill, or clear the stale marker.
◈ XFFI  A plan is marked active but cannot be read — nothing is being kept in scope. Re-run /distill, or clear the stale marker.
◈ XFVE ✗ voice violation — Narrates process extensively ("Let me start by reading," "While that runs," "Meanwhile") with repeated announce-show-confirm scaffolding ("Now...," "Here's the fix," confirming) instead of stating results directly; the information density doesn't justify the length (0.96 terseness user).
         Plain-language voice is the default. Run /xfve-preset-tech to opt into tech mode.
◈ XFVE ✗ voice violation — Uses unexplained technical terms ("log watcher timing out") without translating what it means.
         Plain-language voice is the default. Run /xfve-preset-tech to opt into tech mode.
◈ XFVE ✗ voice violation — "committed" and "push" are git jargon used without translation for a user with high measured frustration on tech-talk.
         Plain-language voice is the default. Run /xfve-preset-tech to opt into tech mode.
◈ XFFI  A plan is marked active but cannot be read — nothing is being kept in scope. Re-run /distill, or clear the stale marker.
