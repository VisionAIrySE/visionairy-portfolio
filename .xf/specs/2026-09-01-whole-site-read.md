# XFFI Spec — Replace the 8-page-capped website read with a whole-site fetch that classifies e
Generated: 2026-09-01
Intent: Replace the 8-page-capped website read with a whole-site fetch that classifies every discovered page by purpose — identity, services, contact, people, hiring, news — where a page may belong to MORE THAN ONE purpose and is read once per purpose it belongs to, runs one focused read per non-empty group in parallel with concurrency tuned on the fly, stops at two minutes per business, stops following any path after ten same-shaped pages EXCEPT beneath the team page, skips calendars and individual product pages and legal boilerplate and logged-in areas while still fetching and recording them and the tools they reveal, limits blog posts to one year, always keeps a partial read marked as partial, fires a reconciliation read only on genuine contradiction or an empty whole-site answer giving it the disputed pages themselves, and produces exactly one immutable reading per visit that sits beside earlier visits with every fetched page's readable text kept and unchanged text stored once
<!-- xfxa-status: unverified -->

## Roots

- src/hoursback/peopleSweep.js
- src/hoursback/understand.js
- scripts/hoursback/understand-businesses.js
- src/hoursback/readings.js
- src/hoursback/recordTheRead.js
- src/hoursback/siteRead.js

## Why this spec exists — the current behaviour being replaced

scripts/hoursback/understand-businesses.js is documented as producing, today, business descriptions and person-to-office placements drawn from roughly four pages, and the note names which of those outputs a whole-site read changes <!-- type:Comprehend --> <!-- signoff: unsigned -->

## Terminals

Build

  whole-site-crawl-replacing-the-page-cap
  - [ ] src/hoursback/peopleSweep.js exports a crawl function that returns every same-host HTML page reachable from the home page, including pages whose path and label match none of the WORTH_OPENING patterns <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
  - [ ] src/hoursback/peopleSweep.js keeps a link scored 99 by rank(url, label) in the crawl queue instead of dropping it in linksToPeople() <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
  - [ ] src/hoursback/peopleSweep.js resolves its page ceiling without the Math.min against MAX_PAGES_PER_SITE, so a caller asking for the whole site reaches MAX_PAGES_WITH_A_TEAM_PAGE or higher <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
  - [ ] src/hoursback/peopleSweep.js returns more than 8 page entries for a site publishing more than 8 same-host HTML pages <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
  - [ ] src/hoursback/peopleSweep.js stops fetching a business after two minutes of elapsed crawl time and returns the pages gathered so far with a partial field set true <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
  - [ ] src/hoursback/peopleSweep.js stops following a path once ten pages of the same path shape have been fetched, and records the shape it stopped on <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
  - [ ] src/hoursback/peopleSweep.js exempts links returned by linksBelow(html, teamPageUrl) for a url accepted by looksLikeATeamPage() from the ten-same-shape stop, so an eleventh staff profile is still fetched <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->

  classify-every-page-by-purpose
  - [ ] src/hoursback/understand.js exports a classify function that returns an array of purposes drawn from identity, services, contact, people, hiring and news for one fetched page <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js returns both contact and people for a page that gives an address and names staff, so a page is never forced into a single purpose <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js returns a purpose array for every page the crawl returned, so the count of classified pages equals the count of fetched pages <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js returns COULD_NOT_TELL for a page it cannot place and writes no default purpose in its place <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js returns, beside each purpose, the url and the text the classification rested on, so a reader can see why a page was called people as well as contact <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js places a page carrying two purposes into both purpose groups, so that page is read once for each purpose it belongs to <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js returns all six purpose keys, with an empty array for a purpose no page matched rather than an omitted key <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js makes zero model calls for a purpose whose page array is empty and records that purpose as COULD_NOT_TELL with the reason that no page of that purpose was found <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->

  one-immutable-reading-per-visit-with-every-page-text-kept
  - [ ] scripts/hoursback/understand-businesses.js calls startReading() once per site visit and passes the returned readingId into every group read of that visit <!-- type:Build --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
  - [ ] scripts/hoursback/understand-businesses.js calls keepPage() once per fetched page, including pages marked skip-from-reading, so a visit that fetched N pages leaves N page rows <!-- type:Build --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
  - [ ] src/hoursback/readings.js keepPage() computes a crypto hash of the page text and links a page whose text matches an already stored hash to that stored copy, so unchanged text is held once rather than duplicated per visit <!-- type:Build --> <!-- check: file_exists | src/hoursback/readings.js | -->
  - [ ] src/hoursback/readings.js keepPage() accepts a marker recording whether the page text was sent to a model, so a page stored but never read is distinguishable from a page that was read <!-- type:Build --> <!-- check: file_exists | src/hoursback/readings.js | -->
  - [ ] scripts/hoursback/understand-businesses.js calls finishReading() with READ, NO_WEBSITE, UNREACHABLE or FAILED for every visit that called startReading(), including visits stopped by the two-minute limit <!-- type:Build --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
  - [ ] scripts/hoursback/understand-businesses.js records a visit stopped early with a partial marker on the reading and keeps every answer that arrived before the stop <!-- type:Build --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
  - [ ] src/hoursback/readings.js everyAnswer() returns the findings of an earlier visit alongside the findings of the new visit, so a new reading sits beside the old one rather than replacing it <!-- type:Build --> <!-- check: file_exists | src/hoursback/readings.js | -->
  - [ ] src/hoursback/recordTheRead.js writes each answered field through record() naming the purpose group that produced it, so two groups answering one field leave two findings rather than one contested value <!-- type:Build --> <!-- check: file_exists | src/hoursback/recordTheRead.js | -->
  - [ ] scripts/hoursback/understand-businesses.js contains no skip-if-unchanged guard and no early return that would stop keepPage() or record() running for a site visited before <!-- type:Build --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
  - [ ] src/hoursback/peopleSweep.js returns every page with a url, a fetch status and the readableText(html) output, and returns no url entry lacking a text field <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
  - [ ] src/hoursback/peopleSweep.js marks a calendar page skip-from-reading and still returns its readableText output <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
  - [ ] src/hoursback/peopleSweep.js marks an individual product page skip-from-reading and still returns its readableText output <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
  - [ ] scripts/hoursback/understand-businesses.js stores every skip-from-reading page with keepPage(), so a wrong skip is recoverable without a second visit <!-- type:Build --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->

  parallel-group-reads-wired-into-understand-businesses
  - [ ] src/hoursback/understand.js exports a per-purpose question builder returning a distinct prompt for each of identity, services, contact, people, hiring and news <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js issues the group reads concurrently, so a site with six non-empty groups has six model calls in flight rather than six run in sequence <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js raises and lowers the number of reads in flight during a run from observed call latency and failures, and reports the level it settled on <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js reads a page belonging to two purposes once in each of those two group reads and records each answer against the group that produced it <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] scripts/hoursback/understand-businesses.js replaces its single pagesAsDocument(pages) and questionAbout(name, document) call with the grouped read and no longer passes { maxPages: PAGES_PER_SITE } into ps.fetchPeoplePages() <!-- type:Build --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
  - [ ] scripts/hoursback/understand-businesses.js runs keepOnlyWhatWasRead() over each group answer against the pages of that same group, so a name or address absent from those pages is dropped before record() sees it <!-- type:Build --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
  - [ ] scripts/hoursback/understand-businesses.js sends every group prompt through the existing askTheReader() call path and awaits the group answers of a visit before calling finishReading() <!-- type:Build --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
  - [ ] scripts/hoursback/understand-businesses.js gains a named ceiling constant bounding how many sites are read at once, and the grouped read never exceeds it <!-- type:Build --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
  - [ ] src/hoursback/understand.js splits a group whose text exceeds its allowance into batches, reads each batch, and returns the merged result as the single answer for that group <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->

  reconciliation-read-when-group-answers-disagree-or-come-back-empty
  - [ ] src/hoursback/understand.js returns the list of fields for which two groups gave different non-empty values, and treats one group answering while another returned nothing as no contradiction <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js fires the reconciliation read when that contradiction list is non-empty or when a field every group was asked came back empty from all of them, and makes zero extra model calls otherwise <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js passes the readable text of the disputed pages themselves into the reconciliation prompt, not only the answers those pages produced <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/recordTheRead.js writes the reconciliation result through record() as its own finding, leaving each group finding it weighed readable through everyAnswer() <!-- type:Build --> <!-- check: file_exists | src/hoursback/recordTheRead.js | -->
  - [ ] src/hoursback/understand.js records COULD_NOT_TELL for a field the reconciliation read cannot settle rather than promoting one group answer as a tie-break default <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js returns a group answer built from part of its batches when the visit runs out of time, marked partial, rather than discarding the batches that finished <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->

  every-fetched-pages-readable-text-reaches-the-model-untruncated
  - [ ] src/hoursback/understand.js gives each purpose group its own character allowance instead of counting all pages against the single TOTAL_CHARS = 20000 budget in pagesAsDocument() <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js removes text blocks repeating across three or more pages of the same site before assembling a group document, so a shared menu and footer no longer consume the allowance <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/understand.js returns a cut marker naming any page whose text it had to shorten, and the full text of that page stays in the row written by keepPage() <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] scripts/hoursback/understand-businesses.js no longer retries a failed read with document.slice(0, 12000) <!-- type:Build --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->

  pages-skipped-by-rule-and-the-tools-they-reveal-recorded
  - [ ] src/hoursback/peopleSweep.js marks legal boilerplate skip-from-reading and still returns its readableText output <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
  - [ ] src/hoursback/peopleSweep.js marks a logged-in area skip-from-reading and still returns its readableText output <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
  - [ ] src/hoursback/peopleSweep.js marks a dated post carrying a date older than the one-year limit skip-from-reading, and follows no link to a still older post below it <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
  - [ ] src/hoursback/peopleSweep.js also marks cart and checkout pages, search-results pages, post index pages and other-language copies skip-from-reading, and returns the readableText output of each <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
  - [ ] src/hoursback/understand.js returns, for each skipped page, the name of the rule that caught it, so a reader can see why that page went unread <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
  - [ ] src/hoursback/siteRead.js runs toolsFromPages() over the skipped pages as well as the read pages and returns every TOOL_FINGERPRINTS name it matched <!-- type:Build --> <!-- check: file_exists | src/hoursback/siteRead.js | -->
  - [ ] src/hoursback/recordTheRead.js writes each detected tool through record() with the system name and the detection method that found it, and keeps both entries when the fingerprint and the model name different systems <!-- type:Build --> <!-- check: file_exists | src/hoursback/recordTheRead.js | -->
  - [ ] src/hoursback/recordTheRead.js writes a finding when a tool recorded on a previous visit appears on no page of this visit, so a system that went away is recorded as loudly as one appearing <!-- type:Build --> <!-- check: file_exists | src/hoursback/recordTheRead.js | -->

Understand
- [ ] src/hoursback/peopleSweep.js contains no Math.min against MAX_PAGES_PER_SITE and no rank() === 99 drop in linksToPeople(), and fetchPeoplePages() lets a whole-site caller reach MAX_PAGES_WITH_A_TEAM_PAGE, so no caller is silently capped by any of the three old ways <!-- type:Comprehend --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
- [ ] src/hoursback/understand.js contains no TOTAL_CHARS constant and no single 20000-character site budget, so the pages reaching the model are no longer stopped at roughly four however many the crawl fetched <!-- type:Comprehend --> <!-- check: file_exists | src/hoursback/understand.js | -->
- [ ] scripts/hoursback/understand-businesses.js contains no PAGES_PER_SITE constant and no questionAbout() call, so no visit is capped at eight pages and no site is answered through one whole-site question — every answer arrives through a per-purpose group read <!-- type:Comprehend --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
- [ ] src/hoursback/understand.js exports SINGLE_GROUP_FIELDS and WHOLE_SITE_FIELDS with every field it answers in exactly one of the two lists, and its reconciliation trigger reads WHOLE_SITE_FIELDS to pick the fields whose empty answer across all groups fires the read <!-- type:Comprehend --> <!-- check: file_exists | src/hoursback/understand.js | -->
- [ ] src/hoursback/peopleSweep.js exports the pathShape() function the ten-page stop counts by, and pathShape() returns one shared key for /products/red-widget and /products/blue-widget, one for /blog/2024/01/first-post and /blog/2023/06/older-post, one for /shop/page/2 and /shop/page/3, and distinct keys for /about and /contact, with links returned by linksBelow(html, teamPageUrl) the only exemption from that stop <!-- type:Comprehend --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
- [ ] src/hoursback/readings.js exports startReading, keepPage, record and finishReading, and startReading() returns a readingId that keepPage(), record() and finishReading() each accept, so one visit is one reading and every group read of that visit shares its readingId <!-- type:Comprehend --> <!-- check: file_exists | src/hoursback/readings.js | -->
- [ ] src/hoursback/readings.js contains no db.finding.delete and no db.reading.delete call, and every db.finding.update in it writes only retiredAt, retiredReason and retiredBy — never value — so record() only appends and a second visit lands beside the first <!-- type:Comprehend --> <!-- check: file_exists | src/hoursback/readings.js | -->
- [ ] src/hoursback/readings.js keepPage() hashes page text through the crypto require at its top, and two keepPage() calls under different readingIds leave one stored text row when the text is identical and a second stored row when the text differs <!-- type:Comprehend --> <!-- check: file_exists | src/hoursback/readings.js | -->

Specify
- [ ] src/hoursback/understand.js constrains every classified page to an array of one or more of identity, services, contact, people, hiring and news, or to COULD_NOT_TELL, and permits the same page in more than one group <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
- [ ] src/hoursback/understand.js reads a page once for each purpose it carries, so the model-call count of a visit equals the number of non-empty purpose groups and never the number of pages <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
- [ ] src/hoursback/peopleSweep.js is bounded by two minutes of crawl time per business and by ten pages of one path shape, with links returned by linksBelow(html, teamPageUrl) exempt from the second bound <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
- [ ] src/hoursback/peopleSweep.js fetches and returns the readableText output of every page a skip rule catches, so no skip rule prevents storage <!-- type:Build --> <!-- check: file_exists | src/hoursback/peopleSweep.js | -->
- [ ] src/hoursback/readings.js is constrained to one startReading() call, one keepPage() call per fetched page, one record() call per finding and one finishReading() call per visit, with no update and no delete of a prior reading <!-- type:Build --> <!-- check: file_exists | src/hoursback/readings.js | -->
- [ ] src/hoursback/readings.js holds one stored copy of a page text whose crypto hash it has already seen, and every visit that fetched that text still has its own page row pointing at the copy <!-- type:Build --> <!-- check: file_exists | src/hoursback/readings.js | -->
- [ ] src/hoursback/understand.js records COULD_NOT_TELL for any field no group answered and writes no default value in its place <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
- [ ] src/hoursback/understand.js marks an incomplete visit partial and keeps its answers, so a partial reading is never discarded and never presented as complete <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
- [ ] src/hoursback/understand.js treats two non-empty differing values for one field as contradiction, treats a field empty across every group asked as an empty whole-site answer, and treats a single silent group as neither <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
- [ ] src/hoursback/siteRead.js returns each tool as a TOOL_FINGERPRINTS name paired with the detection method that found it, and keeps both entries when the fingerprint and the model name different systems <!-- type:Build --> <!-- check: file_exists | src/hoursback/siteRead.js | -->
- [ ] src/hoursback/understand.js sizes the character allowance per purpose group rather than sharing TOTAL_CHARS = 20000 across the site, and records any cut on the reading rather than dropping text silently <!-- type:Build --> <!-- check: file_exists | src/hoursback/understand.js | -->
- [ ] scripts/hoursback/understand-businesses.js carries a ceiling constant matching the standing order named in its roots, and every model call in the grouped read leaves through askTheReader() to the local reader with no OpenRouter and no paid call <!-- type:Build --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->

Operate
- [ ] scripts/hoursback/understand-businesses.js run over a batch reports per site the fetched page count, the count classified into each of the six purposes, the number of model calls made, and whether the visit finished or stopped partial <!-- type:Operate --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
- [ ] scripts/hoursback/understand-businesses.js run over a site of more than eight pages produces one reading whose stored page count, counted from the database, equals its fetched page count <!-- type:Operate --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
- [ ] scripts/hoursback/understand-businesses.js run over a site that takes longer than two minutes finishes that site inside the limit and leaves a reading marked partial with the answers it obtained <!-- type:Operate --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
- [ ] scripts/hoursback/understand-businesses.js run over a site with a large product listing fetches no more than ten pages of that path shape while still fetching every profile under the team page <!-- type:Operate --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
- [ ] scripts/hoursback/understand-businesses.js run over a site with no hiring and no news pages makes zero model calls for those two groups and records COULD_NOT_TELL for their fields <!-- type:Operate --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
- [ ] scripts/hoursback/understand-businesses.js makes zero reconciliation calls on a site whose group answers agree and one on a site where two groups return different non-empty values for one field <!-- type:Operate --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
- [ ] scripts/hoursback/understand-businesses.js logs the reader each call used and the concurrency level it settled on, and the run log shows no OpenRouter call and no paid call <!-- type:Operate --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
- [ ] scripts/hoursback/understand-businesses.js re-run over an unchanged site leaves every earlier finding returned by everyAnswer(), appends the new visit under a separate readingId, and adds no second stored copy of the unchanged page text <!-- type:Operate --> <!-- check: file_exists | scripts/hoursback/understand-businesses.js | -->
